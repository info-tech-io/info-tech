# Этап 6: Развертывание и эксплуатация

## Шаг 6.1: Подготовка сервера

**Цель:** Подготовить сервер для развертывания платформы InfoTech.io

**Системные требования:**
- OS: Ubuntu 22.04 LTS или новее
- RAM: минимум 2GB, рекомендуется 4GB
- Диск: минимум 20GB свободного места  
- CPU: 1 core (достаточно для начала)
- Домен: `infotecha.ru` с настроенными DNS записями

### Подшаг 6.1.1: Базовая настройка сервера

**Действия на сервере:**
```bash
# Обновление системы
sudo apt-get update && sudo apt-get upgrade -y

# Установка необходимых пакетов
sudo apt-get install -y \
    apache2 \
    git \
    curl \
    wget \
    unzip \
    certbot \
    python3-certbot-apache \
    htop \
    tree \
    nano

# Проверка версий
apache2 -v
git --version
```

**Настройка безопасности:**
```bash
# Настройка firewall (UFW)
sudo ufw --force enable
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw status verbose

# Создание пользователя для деплоя
sudo adduser deploy-user --disabled-password --gecos ""
sudo usermod -aG www-data deploy-user

# Настройка SSH ключей для GitHub Actions
sudo mkdir -p /home/deploy-user/.ssh
sudo chown deploy-user:deploy-user /home/deploy-user/.ssh
sudo chmod 700 /home/deploy-user/.ssh

# Добавление SSH ключа (будет добавлен позже через GitHub Secrets)
# echo "ssh-rsa AAAAB3N..." | sudo tee /home/deploy-user/.ssh/authorized_keys
# sudo chmod 600 /home/deploy-user/.ssh/authorized_keys
# sudo chown deploy-user:deploy-user /home/deploy-user/.ssh/authorized_keys
```

**Контрольные процедуры:**
```bash
# Проверка Apache
sudo systemctl status apache2
curl -I http://localhost | head -1

# Проверка firewall
sudo ufw status | grep -E "(80|443|22)"

# Проверка пользователя deploy-user
id deploy-user
groups deploy-user | grep -q www-data && echo "✅ deploy-user in www-data group"

# Проверка доступности домена (выполнять локально)
nslookup infotecha.ru
ping -c 3 infotecha.ru
```

**Критерии успеха:**
- ✅ Apache2 установлен и запущен
- ✅ Firewall настроен правильно
- ✅ Пользователь deploy-user создан
- ✅ Домен указывает на сервер

---

### Подшаг 6.1.2: Настройка структуры директорий

**Создание структуры для платформы:**
```bash
# Создание основных директорий
sudo mkdir -p /var/www/infotecha.ru/{logs,backups}
sudo mkdir -p /opt/infotecha/{scripts,configs}

# Установка прав доступа  
sudo chown -R www-data:www-data /var/www/infotecha.ru
sudo chmod -R 755 /var/www/infotecha.ru

sudo chown -R deploy-user:deploy-user /opt/infotecha
sudo chmod -R 755 /opt/infotecha

# Создание символической ссылки для удобства
sudo ln -sf /var/www/infotecha.ru /opt/infotecha/webroot

echo "🗂️ Directory structure:"
tree /var/www/infotecha.ru -L 2
tree /opt/infotecha -L 2
```

**Создание временной заглушки:**
```bash
# Создание временной главной страницы
sudo tee /var/www/infotecha.ru/index.html > /dev/null << 'EOF'
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>InfoTech.io - Coming Soon</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            color: white;
        }
        .container {
            text-align: center;
            max-width: 600px;
            padding: 2rem;
        }
        .logo {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 1rem;
            color: #fff;
        }
        .subtitle {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }
        .message {
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
            opacity: 0.8;
        }
        .status {
            background: rgba(255,255,255,0.2);
            padding: 1rem 2rem;
            border-radius: 0.5rem;
            display: inline-block;
        }
        .github {
            margin-top: 2rem;
        }
        .github a {
            color: #fff;
            text-decoration: none;
            font-weight: 500;
        }
        .github a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">InfoTech.io</div>
        <div class="subtitle">Открываем двери в мир технологий</div>
        <div class="message">
            Мы готовим для вас интерактивную образовательную платформу 
            с открытым контентом. Совсем скоро здесь появятся курсы 
            по современным технологиям.
        </div>
        <div class="status">
            🚀 Платформа в разработке
        </div>
        <div class="github">
            <a href="https://github.com/info-tech-io" target="_blank">
                GitHub Organization
            </a>
        </div>
    </div>
</body>
</html>
EOF

# Установка прав
sudo chown www-data:www-data /var/www/infotecha.ru/index.html
sudo chmod 644 /var/www/infotecha.ru/index.html
```

**Контрольные процедуры:**
```bash
# Проверка структуры директорий
test -d /var/www/infotecha.ru && echo "✅ Web directory created"
test -d /opt/infotecha && echo "✅ App directory created"

# Проверка прав доступа
ls -la /var/www/infotecha.ru/ | head -2
ls -la /opt/infotecha/ | head -2

# Проверка заглушки
curl -s http://localhost | grep -q "InfoTech.io" && echo "✅ Placeholder page works"

# Тест прав записи для deploy-user
sudo -u deploy-user touch /opt/infotecha/test_write && echo "✅ deploy-user can write" && rm /opt/infotecha/test_write
```

**Критерии успеха:**
- ✅ Структура директорий создана
- ✅ Права доступа настроены корректно
- ✅ Временная заглушка отображается
- ✅ deploy-user может записывать файлы

---

## Шаг 6.2: Настройка Apache2 с поддержкой поддоменов

**Цель:** Настроить Apache2 для обслуживания главной страницы и поддоменов модулей

### Подшаг 6.2.1: Создание VirtualHost конфигурации

**Создание основного VirtualHost:**
```bash
# Создание конфигурационного файла
sudo tee /etc/apache2/sites-available/infotecha.conf > /dev/null << 'EOF'
# InfoTech.io Platform VirtualHost Configuration
# Обслуживает главную страницу и все поддомены модулей

<VirtualHost *:80>
    ServerName infotecha.ru
    ServerAlias *.infotecha.ru
    DocumentRoot /var/www/infotecha.ru
    
    # Директория для главного домена
    <Directory /var/www/infotecha.ru>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        DirectoryIndex index.html
        
        # Security headers
        Header always set X-Content-Type-Options nosniff
        Header always set X-Frame-Options DENY
        Header always set X-XSS-Protection "1; mode=block"
        Header always set Referrer-Policy "strict-origin-when-cross-origin"
    </Directory>
    
    # Включаем mod_rewrite
    RewriteEngine On
    
    # Правило для поддоменов: subdomain.infotecha.ru -> /subdomain/
    RewriteCond %{HTTP_HOST} ^([a-z0-9\-]+)\.infotecha\.ru$ [NC]
    RewriteCond %{HTTP_HOST} !^(www|mail|ftp|admin)\.infotecha\.ru$ [NC]  
    RewriteCond %{DOCUMENT_ROOT}/%1 -d
    RewriteRule ^(.*)$ /%1/$1 [L]
    
    # Перенаправление www на основной домен
    RewriteCond %{HTTP_HOST} ^www\.infotecha\.ru$ [NC]
    RewriteRule ^(.*)$ https://infotecha.ru$1 [R=301,L]
    
    # Логирование
    ErrorLog /var/www/infotecha.ru/logs/error.log
    CustomLog /var/www/infotecha.ru/logs/access.log combined
    
    # Максимальный размер загружаемых файлов
    LimitRequestBody 10485760  # 10MB
</VirtualHost>

# HTTPS VirtualHost (будет активирован после получения SSL сертификата)
<VirtualHost *:443>
    ServerName infotecha.ru
    ServerAlias *.infotecha.ru
    DocumentRoot /var/www/infotecha.ru
    
    # SSL конфигурация (настраивается автоматически через Certbot)
    # SSLEngine on
    # SSLCertificateFile /etc/letsencrypt/live/infotecha.ru/fullchain.pem
    # SSLCertificateKeyFile /etc/letsencrypt/live/infotecha.ru/privkey.pem
    
    # Идентичная конфигурация для HTTPS
    <Directory /var/www/infotecha.ru>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        DirectoryIndex index.html
        
        # Дополнительные security headers для HTTPS
        Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
        Header always set X-Content-Type-Options nosniff
        Header always set X-Frame-Options DENY
        Header always set X-XSS-Protection "1; mode=block"
        Header always set Referrer-Policy "strict-origin-when-cross-origin"
    </Directory>
    
    RewriteEngine On
    RewriteCond %{HTTP_HOST} ^([a-z0-9\-]+)\.infotecha\.ru$ [NC]
    RewriteCond %{HTTP_HOST} !^(www|mail|ftp|admin)\.infotecha\.ru$ [NC]
    RewriteCond %{DOCUMENT_ROOT}/%1 -d
    RewriteRule ^(.*)$ /%1/$1 [L]
    
    RewriteCond %{HTTP_HOST} ^www\.infotecha\.ru$ [NC]
    RewriteRule ^(.*)$ https://infotecha.ru$1 [R=301,L]
    
    ErrorLog /var/www/infotecha.ru/logs/error.log
    CustomLog /var/www/infotecha.ru/logs/access.log combined
</VirtualHost>
EOF

# Включение необходимых модулей Apache
sudo a2enmod rewrite
sudo a2enmod headers  
sudo a2enmod ssl

# Отключение стандартного сайта и включение нашего
sudo a2dissite 000-default
sudo a2ensite infotecha

# Проверка конфигурации
sudo apache2ctl configtest

# Перезагрузка Apache
sudo systemctl reload apache2
```

**Создание скрипта для управления модулями:**
```bash
# Создание скрипта для добавления/удаления модулей
sudo tee /opt/infotecha/scripts/manage-module.sh > /dev/null << 'EOF'
#!/bin/bash

# InfoTech.io Module Management Script
# Usage: ./manage-module.sh [add|remove] <module_name>

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
WEBROOT="/var/www/infotecha.ru"
LOG_FILE="/var/www/infotecha.ru/logs/module-management.log"

# Logging function
log() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" | tee -a "$LOG_FILE"
}

# Add module directory
add_module() {
    local module_name="$1"
    local module_dir="$WEBROOT/$module_name"
    
    log "Adding module: $module_name"
    
    # Create module directory if it doesn't exist
    if [ ! -d "$module_dir" ]; then
        mkdir -p "$module_dir"
        chown www-data:www-data "$module_dir"
        chmod 755 "$module_dir"
        log "Created directory: $module_dir"
    else
        log "Directory already exists: $module_dir"
    fi
    
    # Create placeholder if no content
    if [ ! -f "$module_dir/index.html" ]; then
        cat > "$module_dir/index.html" << HTML
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>$module_name - InfoTech.io</title>
    <style>
        body { font-family: sans-serif; text-align: center; padding: 2rem; }
        .message { background: #f0f8ff; padding: 2rem; border-radius: 8px; }
    </style>
</head>
<body>
    <div class="message">
        <h1>$module_name</h1>
        <p>Модуль готовится к публикации...</p>
        <p><a href="https://infotecha.ru">← Вернуться на главную</a></p>
    </div>
</body>
</html>
HTML
        chown www-data:www-data "$module_dir/index.html"
        chmod 644 "$module_dir/index.html"
        log "Created placeholder for: $module_name"
    fi
    
    log "Module $module_name added successfully"
}

# Remove module directory
remove_module() {
    local module_name="$1"
    local module_dir="$WEBROOT/$module_name"
    
    log "Removing module: $module_name"
    
    if [ -d "$module_dir" ]; then
        # Create backup before removal
        backup_dir="/var/www/infotecha.ru/backups/modules/$module_name-$(date +%Y%m%d_%H%M%S)"
        mkdir -p "$(dirname "$backup_dir")"
        cp -r "$module_dir" "$backup_dir"
        log "Backup created: $backup_dir"
        
        # Remove module directory
        rm -rf "$module_dir"
        log "Removed directory: $module_dir"
    else
        log "Directory does not exist: $module_dir"
    fi
    
    log "Module $module_name removed successfully"
}

# Main script logic
case "$1" in
    add)
        if [ -z "$2" ]; then
            echo "Usage: $0 add <module_name>"
            exit 1
        fi
        add_module "$2"
        ;;
    remove)
        if [ -z "$2" ]; then
            echo "Usage: $0 remove <module_name>"
            exit 1
        fi
        remove_module "$2"
        ;;
    *)
        echo "Usage: $0 [add|remove] <module_name>"
        echo ""
        echo "Examples:"
        echo "  $0 add linux-base"
        echo "  $0 remove old-module"
        exit 1
        ;;
esac

# Reload Apache to apply changes
systemctl reload apache2
log "Apache reloaded"
EOF

# Установка прав на скрипт
sudo chmod +x /opt/infotecha/scripts/manage-module.sh
sudo chown deploy-user:deploy-user /opt/infotecha/scripts/manage-module.sh
```

**Контрольные процедуры:**
```bash
# Проверка конфигурации Apache
sudo apache2ctl configtest

# Проверка включенных модулей
apache2ctl -M | grep -E "(rewrite|headers|ssl)"

# Проверка включенных сайтов
sudo a2ensite infotecha && echo "✅ Site enabled" || echo "❌ Site enable failed"

# Тест основного домена  
curl -I http://localhost | grep -q "200 OK" && echo "✅ Main site works"

# Тест скрипта управления модулями
sudo /opt/infotecha/scripts/manage-module.sh add test-module
curl -s http://localhost/test-module/ | grep -q "test-module" && echo "✅ Module management works"
sudo /opt/infotecha/scripts/manage-module.sh remove test-module

# Проверка логов
ls -la /var/www/infotecha.ru/logs/
tail -5 /var/www/infotecha.ru/logs/module-management.log
```

**Критерии успеха:**
- ✅ VirtualHost конфигурация создана
- ✅ Модули Apache включены
- ✅ Основной сайт доступен
- ✅ Скрипт управления модулями работает

---

### Подшаг 6.2.2: Настройка SSL сертификатов

**Получение SSL сертификата через Let's Encrypt:**
```bash
# Получение сертификата для основного домена и wildcard поддоменов
# ВНИМАНИЕ: Wildcard сертификаты требуют DNS-01 challenge

# Вариант 1: Сертификат только для основного домена (простой)
sudo certbot --apache -d infotecha.ru -d www.infotecha.ru --non-interactive --agree-tos --email admin@infotecha.ru

# Вариант 2: Wildcard сертификат (требует настройки DNS)
# sudo certbot certonly --manual --preferred-challenges=dns \
#   -d infotecha.ru -d "*.infotecha.ru" \
#   --email admin@infotecha.ru --agree-tos --non-interactive

# После получения сертификата, автоматически обновим конфигурацию
sudo certbot --apache -d infotecha.ru --non-interactive

# Настройка автоматического обновления
sudo systemctl enable certbot.timer
sudo systemctl start certbot.timer

# Тест автообновления
sudo certbot renew --dry-run
```

**Создание скрипта для получения сертификатов поддоменов:**
```bash
# Скрипт для получения сертификатов для новых модулей
sudo tee /opt/infotecha/scripts/setup-ssl.sh > /dev/null << 'EOF'
#!/bin/bash

# SSL Certificate Setup for InfoTech.io modules
# Usage: ./setup-ssl.sh <module_subdomain>

set -e

SUBDOMAIN="$1"
DOMAIN="infotecha.ru"
FULL_DOMAIN="${SUBDOMAIN}.${DOMAIN}"

if [ -z "$SUBDOMAIN" ]; then
    echo "Usage: $0 <subdomain>"
    echo "Example: $0 linux-base"
    exit 1
fi

echo "🔐 Setting up SSL certificate for: $FULL_DOMAIN"

# Check if certificate already exists
if [ -d "/etc/letsencrypt/live/$FULL_DOMAIN" ]; then
    echo "✅ Certificate already exists for $FULL_DOMAIN"
    exit 0
fi

# Get certificate using Apache plugin
certbot --apache -d "$FULL_DOMAIN" \
    --non-interactive \
    --agree-tos \
    --email admin@infotecha.ru \
    --redirect

if [ $? -eq 0 ]; then
    echo "✅ SSL certificate obtained for $FULL_DOMAIN"
    systemctl reload apache2
    echo "🔄 Apache reloaded"
else
    echo "❌ Failed to obtain SSL certificate"
    exit 1
fi
EOF

sudo chmod +x /opt/infotecha/scripts/setup-ssl.sh
sudo chown deploy-user:deploy-user /opt/infotecha/scripts/setup-ssl.sh
```

**Контрольные процедуры:**
```bash
# Проверка статуса certbot
sudo certbot certificates

# Проверка SSL конфигурации
sudo apache2ctl configtest

# Проверка HTTPS доступа
curl -I https://infotecha.ru | head -1

# Проверка перенаправления HTTP -> HTTPS
curl -I http://infotecha.ru | grep -q "301\|302" && echo "✅ HTTP redirect works"

# Проверка автоматического обновления
sudo systemctl status certbot.timer

# Тест скрипта SSL (если есть тестовый поддомен)
# sudo /opt/infotecha/scripts/setup-ssl.sh test-ssl
```

**Критерии успеха:**
- ✅ SSL сертификат получен для основного домена
- ✅ HTTPS редирект настроен
- ✅ Автоматическое обновление сертификатов включено
- ✅ Скрипт для новых поддоменов готов

---

## Шаг 6.3: Настройка GitHub Actions Secrets

**Цель:** Настроить секреты для автоматического деплоя через GitHub Actions

### Подшаг 6.3.1: Создание SSH ключей

**Генерация SSH ключей для деплоя:**
```bash
# На локальной машине или сервере (временно)
ssh-keygen -t ed25519 -C "github-actions-deploy@infotecha.ru" -f ~/.ssh/infotecha_deploy -N ""

# Просмотр ключей
echo "=== PUBLIC KEY (добавить в authorized_keys на сервере) ==="
cat ~/.ssh/infotecha_deploy.pub
echo ""
echo "=== PRIVATE KEY (добавить в GitHub Secrets как PROD_SSH_KEY) ==="
cat ~/.ssh/infotecha_deploy
echo ""

# Добавление публичного ключа на сервер
# (выполнять на сервере)
sudo mkdir -p /home/deploy-user/.ssh
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleKeyHere github-actions-deploy@infotecha.ru" | \
  sudo tee -a /home/deploy-user/.ssh/authorized_keys
sudo chmod 600 /home/deploy-user/.ssh/authorized_keys
sudo chown deploy-user:deploy-user /home/deploy-user/.ssh/authorized_keys

# Удаление ключей с локальной машины (после добавления в GitHub)
# rm ~/.ssh/infotecha_deploy*
```

### Подшаг 6.3.2: Настройка GitHub Secrets

**Добавление секретов в GitHub:**

**В репозитории `infotecha` настроить следующие секреты:**

1. **Repository Secrets** (`Settings > Secrets and variables > Actions`):
```
PROD_HOST=YOUR_SERVER_IP_OR_DOMAIN
PROD_USERNAME=deploy-user  
PROD_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----
[содержимое приватного SSH ключа]
-----END OPENSSH PRIVATE KEY-----

PAT_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
(GitHub Personal Access Token с правами repo, workflow)
```

2. **Environment Secrets** (создать Environment "production"):
```
Environment: production
Secrets:
- PROD_HOST=your.server.ip
- PROD_USERNAME=deploy-user
- PROD_SSH_KEY=[private key content]
```

**Создание тестового workflow для проверки подключения:**
```bash
# В репозитории infotecha создать файл:
# .github/workflows/test-deployment.yml
```

```yaml
name: Test Deployment Connection

on:
  workflow_dispatch:
    inputs:
      test_message:
        description: 'Test message to deploy'
        required: false
        default: 'Connection test'

jobs:
  test-connection:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - name: Test SSH Connection
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.PROD_HOST }}
        username: ${{ secrets.PROD_USERNAME }}
        key: ${{ secrets.PROD_SSH_KEY }}
        script: |
          echo "🎉 SSH connection successful!"
          echo "Server: $(hostname)"
          echo "User: $(whoami)"
          echo "Date: $(date)"
          echo "Test message: ${{ github.event.inputs.test_message }}"
          
          # Test web directory access
          ls -la /var/www/infotecha.ru/ | head -5
          
          # Test module management script
          if [ -f "/opt/infotecha/scripts/manage-module.sh" ]; then
              echo "✅ Module management script found"
          else
              echo "❌ Module management script missing"
          fi
          
          # Test Apache status
          systemctl is-active apache2 && echo "✅ Apache is running" || echo "❌ Apache not running"
```

### Подшаг 6.3.3: Создание скриптов деплоя на сервере

**Создание главного скрипта деплоя:**
```bash
# Создание скрипта деплоя на сервере
sudo tee /opt/infotecha/scripts/deploy.sh > /dev/null << 'EOF'
#!/bin/bash

# InfoTech.io Deployment Script
# Handles deployment of platform hub and modules

set -e

WEBROOT="/var/www/infotecha.ru"
BACKUP_DIR="/var/www/infotecha.ru/backups"
LOG_FILE="/var/www/infotecha.ru/logs/deployment.log"
TEMP_DIR="/tmp/infotecha-deploy-$(date +%Y%m%d_%H%M%S)"

# Logging function
log() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" | tee -a "$LOG_FILE"
}

# Error handler
error_handler() {
    log "❌ ERROR: Deployment failed at line $1"
    exit 1
}

trap 'error_handler $LINENO' ERR

# Function: Deploy hub (main page and modules.json)
deploy_hub() {
    log "🚀 Starting hub deployment..."
    
    # Create backup of current files
    if [ -f "$WEBROOT/index.html" ]; then
        local backup_name="hub-backup-$(date +%Y%m%d_%H%M%S)"
        mkdir -p "$BACKUP_DIR/$backup_name"
        cp "$WEBROOT/index.html" "$BACKUP_DIR/$backup_name/" 2>/dev/null || true
        cp "$WEBROOT/modules.json" "$BACKUP_DIR/$backup_name/" 2>/dev/null || true
        cp "$WEBROOT"/*.css "$BACKUP_DIR/$backup_name/" 2>/dev/null || true
        cp "$WEBROOT"/*.js "$BACKUP_DIR/$backup_name/" 2>/dev/null || true
        log "📦 Backup created: $backup_name"
    fi
    
    # Deploy new files (files should be uploaded to $TEMP_DIR by GitHub Actions)
    if [ -d "$TEMP_DIR" ]; then
        log "📁 Deploying files from: $TEMP_DIR"
        
        # Copy files with proper permissions
        find "$TEMP_DIR" -type f -name "*.html" -exec cp {} "$WEBROOT/" \;
        find "$TEMP_DIR" -type f -name "*.css" -exec cp {} "$WEBROOT/" \;
        find "$TEMP_DIR" -type f -name "*.js" -exec cp {} "$WEBROOT/" \;
        find "$TEMP_DIR" -type f -name "modules.json" -exec cp {} "$WEBROOT/" \;
        
        # Copy images directory if exists
        if [ -d "$TEMP_DIR/images" ]; then
            cp -r "$TEMP_DIR/images" "$WEBROOT/" 2>/dev/null || true
        fi
        
        # Set proper permissions
        chown -R www-data:www-data "$WEBROOT"/*.html "$WEBROOT"/*.css "$WEBROOT"/*.js "$WEBROOT"/modules.json 2>/dev/null || true
        chown -R www-data:www-data "$WEBROOT/images" 2>/dev/null || true
        chmod 644 "$WEBROOT"/*.html "$WEBROOT"/*.css "$WEBROOT"/*.js "$WEBROOT"/modules.json 2>/dev/null || true
        
        log "✅ Hub files deployed successfully"
    else
        log "⚠️ No temp directory found, skipping file deployment"
    fi
    
    # Validate deployment
    if [ -f "$WEBROOT/index.html" ] && [ -f "$WEBROOT/modules.json" ]; then
        log "✅ Hub deployment validation passed"
    else
        log "❌ Hub deployment validation failed"
        return 1
    fi
}

# Function: Deploy specific module
deploy_module() {
    local module_name="$1"
    local module_subdir="$2"
    
    if [ -z "$module_name" ] || [ -z "$module_subdir" ]; then
        log "❌ Module name and subdirectory required"
        return 1
    fi
    
    log "🚀 Starting module deployment: $module_name"
    
    local module_dir="$WEBROOT/$module_subdir"
    
    # Create module directory if doesn't exist
    mkdir -p "$module_dir"
    
    # Create backup
    if [ "$(ls -A $module_dir 2>/dev/null)" ]; then
        local backup_name="module-${module_name}-backup-$(date +%Y%m%d_%H%M%S)"
        mkdir -p "$BACKUP_DIR/$backup_name"
        cp -r "$module_dir"/* "$BACKUP_DIR/$backup_name/" 2>/dev/null || true
        log "📦 Module backup created: $backup_name"
    fi
    
    # Deploy module files
    local module_temp="$TEMP_DIR/$module_name"
    if [ -d "$module_temp" ]; then
        log "📁 Deploying module from: $module_temp"
        
        # Clear existing content and deploy new
        rm -rf "$module_dir"/*
        cp -r "$module_temp"/* "$module_dir/"
        
        # Set permissions
        chown -R www-data:www-data "$module_dir"
        find "$module_dir" -type f -exec chmod 644 {} \;
        find "$module_dir" -type d -exec chmod 755 {} \;
        
        log "✅ Module $module_name deployed successfully"
    else
        log "❌ Module source directory not found: $module_temp"
        return 1
    fi
    
    # Validate module deployment
    if [ -f "$module_dir/index.html" ]; then
        log "✅ Module deployment validation passed"
    else
        log "❌ Module deployment validation failed - no index.html"
        return 1
    fi
}

# Function: Reload web server
reload_webserver() {
    log "🔄 Reloading Apache..."
    
    # Test configuration first
    if apache2ctl configtest; then
        systemctl reload apache2
        log "✅ Apache reloaded successfully"
    else
        log "❌ Apache configuration test failed"
        return 1
    fi
}

# Function: Cleanup
cleanup() {
    log "🧹 Cleaning up temporary files..."
    rm -rf "$TEMP_DIR" 2>/dev/null || true
    
    # Clean old backups (keep last 10)
    if [ -d "$BACKUP_DIR" ]; then
        ls -1t "$BACKUP_DIR" | tail -n +11 | head -20 | while read old_backup; do
            rm -rf "$BACKUP_DIR/$old_backup"
            log "🗑️ Removed old backup: $old_backup"
        done
    fi
    
    log "✅ Cleanup completed"
}

# Main deployment logic
main() {
    local deployment_type="${1:-hub}"
    local module_name="$2"
    local module_subdir="$3"
    
    log "🚀 Starting InfoTech.io deployment..."
    log "Type: $deployment_type"
    log "Module: ${module_name:-N/A}"
    log "Subdirectory: ${module_subdir:-N/A}"
    
    case "$deployment_type" in
        "hub")
            deploy_hub
            ;;
        "module") 
            deploy_module "$module_name" "$module_subdir"
            ;;
        *)
            log "❌ Invalid deployment type: $deployment_type"
            log "Usage: $0 [hub|module] [module_name] [module_subdir]"
            exit 1
            ;;
    esac
    
    reload_webserver
    cleanup
    
    log "🎉 Deployment completed successfully!"
}

# Run main function with all arguments
main "$@"
EOF

# Установка прав
sudo chmod +x /opt/infotecha/scripts/deploy.sh
sudo chown deploy-user:deploy-user /opt/infotecha/scripts/deploy.sh
```

**Контрольные процедуры:**
```bash
# Проверка SSH ключей на сервере
ls -la /home/deploy-user/.ssh/
cat /home/deploy-user/.ssh/authorized_keys | grep -q "github-actions" && echo "✅ SSH key installed"

# Тест SSH подключения (с локальной машины)
ssh -i ~/.ssh/infotecha_deploy deploy-user@YOUR_SERVER_IP "echo 'SSH connection works'"

# Проверка скриптов
ls -la /opt/infotecha/scripts/
test -x /opt/infotecha/scripts/deploy.sh && echo "✅ Deploy script executable"

# Тест workflow (запустить вручную в GitHub Actions)
# Проверить в репозитории infotecha -> Actions -> Test Deployment Connection

# Проверка логов
tail -10 /var/www/infotecha.ru/logs/deployment.log 2>/dev/null || echo "No deployment logs yet"
```

**Критерии успеха:**
- ✅ SSH ключи настроены корректно
- ✅ GitHub Secrets добавлены
- ✅ Тестовый workflow проходит
- ✅ Скрипты деплоя готовы к использованию

---

## Шаг 6.4: Первоначальное развертывание платформы

**Цель:** Развернуть репозиторий infotecha на сервере

### Подшаг 6.4.1: Ручной деплой для инициализации

**Клонирование и первичное развертывание:**
```bash
# На сервере (от имени deploy-user)
su - deploy-user

# Клонирование репозитория infotecha (временно, для первичного деплоя)
git clone https://github.com/info-tech-io/infotecha.git /tmp/infotecha-initial
cd /tmp/infotecha-initial

# Копирование файлов hub
sudo cp content/index.html /var/www/infotecha.ru/
sudo cp content/*.css /var/www/infotecha.ru/ 2>/dev/null || true
sudo cp content/*.js /var/www/infotecha.ru/ 2>/dev/null || true
sudo cp modules.json /var/www/infotecha.ru/

# Копирование изображений если есть
sudo cp -r content/images /var/www/infotecha.ru/ 2>/dev/null || true

# Установка прав
sudo chown -R www-data:www-data /var/www/infotecha.ru/{index.html,*.css,*.js,modules.json}
sudo chown -R www-data:www-data /var/www/infotecha.ru/images 2>/dev/null || true

# Очистка
rm -rf /tmp/infotecha-initial

echo "✅ Initial deployment completed"
```

### Подшаг 6.4.2: Создание тестового модуля

**Создание тестового модуля для проверки поддоменов:**
```bash
# Создание директории тестового модуля
sudo /opt/infotecha/scripts/manage-module.sh add test-module

# Создание расширенной тестовой страницы
sudo tee /var/www/infotecha.ru/test-module/index.html > /dev/null << 'EOF'
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Тестовый модуль - InfoTech.io</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
            line-height: 1.6;
            color: #333;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 2rem;
            border-radius: 1rem;
            text-align: center;
            margin-bottom: 2rem;
        }
        .section {
            background: #f8f9fa;
            padding: 1.5rem;
            border-radius: 0.5rem;
            margin-bottom: 1.5rem;
            border: 1px solid #e9ecef;
        }
        .status {
            display: inline-block;
            padding: 0.5rem 1rem;
            background: #d4edda;
            color: #155724;
            border-radius: 0.25rem;
            font-weight: 500;
        }
        .nav-link {
            color: #007bff;
            text-decoration: none;
            font-weight: 500;
        }
        .nav-link:hover {
            text-decoration: underline;
        }
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-top: 1rem;
        }
        .info-item {
            background: white;
            padding: 1rem;
            border-radius: 0.5rem;
            border: 1px solid #dee2e6;
        }
        .info-label {
            font-weight: 600;
            color: #6c757d;
            font-size: 0.875rem;
        }
        .info-value {
            font-size: 1.1rem;
            margin-top: 0.25rem;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🧪 Тестовый модуль</h1>
        <p>Проверка работы поддоменов InfoTech.io платформы</p>
    </div>

    <div class="section">
        <h2>Статус системы</h2>
        <div class="status">✅ Поддомен работает корректно</div>
        <p>Этот модуль доступен по адресу: <strong>test-module.infotecha.ru</strong></p>
        <p>Apache mod_rewrite правильно перенаправляет трафик в подпапку модуля.</p>
    </div>

    <div class="section">
        <h2>Информация о развертывании</h2>
        <div class="info-grid">
            <div class="info-item">
                <div class="info-label">Дата создания</div>
                <div class="info-value" id="creation-date">Загрузка...</div>
            </div>
            <div class="info-item">
                <div class="info-label">Время загрузки</div>
                <div class="info-value" id="load-time">Загрузка...</div>
            </div>
            <div class="info-item">
                <div class="info-label">User Agent</div>
                <div class="info-value" id="user-agent">Загрузка...</div>
            </div>
            <div class="info-item">
                <div class="info-label">Протокол</div>
                <div class="info-value" id="protocol">Загрузка...</div>
            </div>
        </div>
    </div>

    <div class="section">
        <h2>Тест функций</h2>
        <p>Этот модуль демонстрирует:</p>
        <ul>
            <li>✅ Работу поддоменов через Apache mod_rewrite</li>
            <li>✅ Правильную структуру файлов модуля</li>
            <li>✅ Responsive дизайн</li>
            <li>✅ JavaScript функциональность</li>
        </ul>
    </div>

    <div class="section">
        <h2>Навигация</h2>
        <p>
            <a href="https://infotecha.ru" class="nav-link">🏠 Главная страница</a> |
            <a href="https://github.com/info-tech-io" class="nav-link">📁 GitHub Organization</a> |
            <a href="https://github.com/info-tech-io/infotecha" class="nav-link">⚙️ Исходный код платформы</a>
        </p>
    </div>

    <script>
        // JavaScript для демонстрации работы
        document.addEventListener('DOMContentLoaded', function() {
            // Установка даты создания
            document.getElementById('creation-date').textContent = new Date().toLocaleDateString('ru-RU');
            
            // Время загрузки страницы
            document.getElementById('load-time').textContent = (Date.now() - performance.timing.navigationStart) + ' мс';
            
            // User Agent
            document.getElementById('user-agent').textContent = navigator.userAgent.substring(0, 60) + '...';
            
            // Протокол
            document.getElementById('protocol').textContent = location.protocol === 'https:' ? 'HTTPS ✅' : 'HTTP ⚠️';
            
            console.log('🧪 Тестовый модуль InfoTech.io загружен успешно');
        });
    </script>
</body>
</html>
EOF

# Установка прав
sudo chown www-data:www-data /var/www/infotecha.ru/test-module/index.html
sudo chmod 644 /var/www/infotecha.ru/test-module/index.html
```

### Подшаг 6.4.3: Финальное тестирование

**Комплексное тестирование платформы:**
```bash
# Создание скрипта комплексного тестирования
sudo tee /opt/infotecha/scripts/test-platform.sh > /dev/null << 'EOF'
#!/bin/bash

# InfoTech.io Platform Testing Script

echo "🧪 InfoTech.io Platform Test Suite"
echo "=================================="

# Colors for output
GREEN='\033[0;32m'
RED='\033[0;31m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

# Test counter
TESTS_TOTAL=0
TESTS_PASSED=0

# Test function
run_test() {
    local test_name="$1"
    local test_command="$2"
    
    TESTS_TOTAL=$((TESTS_TOTAL + 1))
    printf "%-50s" "$test_name"
    
    if eval "$test_command" &>/dev/null; then
        echo -e "${GREEN}✅ PASS${NC}"
        TESTS_PASSED=$((TESTS_PASSED + 1))
    else
        echo -e "${RED}❌ FAIL${NC}"
    fi
}

echo ""
echo "🔧 System Tests"
echo "--------------"

run_test "Apache service status" "systemctl is-active apache2"
run_test "Apache configuration valid" "apache2ctl configtest"
run_test "Web directory exists" "test -d /var/www/infotecha.ru"
run_test "Web directory writable" "test -w /var/www/infotecha.ru"
run_test "SSL certificate exists" "test -d /etc/letsencrypt/live/infotecha.ru"

echo ""
echo "🌐 HTTP/HTTPS Tests"
echo "------------------"

run_test "Main site HTTP response" "curl -s -o /dev/null -w '%{http_code}' http://localhost | grep -q '200'"
run_test "Main site HTTPS response" "curl -s -k -o /dev/null -w '%{http_code}' https://localhost | grep -q '200'"
run_test "Main site contains InfoTech.io" "curl -s http://localhost | grep -q 'InfoTech.io'"
run_test "modules.json accessible" "curl -s http://localhost/modules.json | jq empty"

echo ""
echo "🎯 Module Tests"
echo "---------------"

run_test "Test module directory exists" "test -d /var/www/infotecha.ru/test-module"
run_test "Test module index.html exists" "test -f /var/www/infotecha.ru/test-module/index.html"
run_test "Test module HTTP response" "curl -s -o /dev/null -w '%{http_code}' http://localhost/test-module/ | grep -q '200'"
run_test "Module management script exists" "test -x /opt/infotecha/scripts/manage-module.sh"

echo ""
echo "📁 File Permissions Tests"
echo "-------------------------"

run_test "Web files owned by www-data" "stat -c %U /var/www/infotecha.ru/index.html | grep -q www-data"
run_test "Deployment script executable" "test -x /opt/infotecha/scripts/deploy.sh"
run_test "Log directory writable" "test -w /var/www/infotecha.ru/logs"
run_test "Backup directory exists" "test -d /var/www/infotecha.ru/backups"

echo ""
echo "🔐 Security Tests"  
echo "----------------"

run_test "Directory listing disabled" "! curl -s http://localhost/ | grep -q 'Index of'"
run_test "Security headers present" "curl -s -I http://localhost | grep -q 'X-Content-Type-Options'"
run_test "Firewall enabled" "ufw status | grep -q 'Status: active'"
run_test "SSH on non-standard port or restricted" "true"  # Placeholder

echo ""
echo "📊 Test Results Summary"
echo "======================"
echo -e "Total tests: ${TESTS_TOTAL}"
echo -e "Passed: ${GREEN}${TESTS_PASSED}${NC}"
echo -e "Failed: ${RED}$((TESTS_TOTAL - TESTS_PASSED))${NC}"

if [ $TESTS_PASSED -eq $TESTS_TOTAL ]; then
    echo -e "\n🎉 ${GREEN}All tests passed! Platform is ready for production.${NC}"
    exit 0
else
    echo -e "\n⚠️ ${YELLOW}Some tests failed. Please review the configuration.${NC}"
    exit 1
fi
EOF

sudo chmod +x /opt/infotecha/scripts/test-platform.sh
sudo chown deploy-user:deploy-user /opt/infotecha/scripts/test-platform.sh

# Запуск тестов
sudo /opt/infotecha/scripts/test-platform.sh
```

**Контрольные процедуры финального тестирования:**
```bash
# 1. Проверка основных URL
curl -I https://infotecha.ru | head -1
curl -I https://test-module.infotecha.ru | head -1

# 2. Проверка modules.json
curl -s https://infotecha.ru/modules.json | jq '.modules | keys'

# 3. Проверка редиректов
curl -I http://infotecha.ru | grep Location

# 4. Проверка логов
tail -5 /var/www/infotecha.ru/logs/access.log
tail -5 /var/www/infotecha.ru/logs/error.log

# 5. Проверка SSL
openssl s_client -connect infotecha.ru:443 -servername infotecha.ru < /dev/null 2>/dev/null | openssl x509 -noout -dates

# 6. Тест GitHub Actions деплоя (запустить workflow)
# В репозитории infotecha -> Actions -> Deploy Hub -> Run workflow

echo "🎯 Manual verification checklist:"
echo "1. Visit https://infotecha.ru - should show main page"
echo "2. Visit https://test-module.infotecha.ru - should show test module"  
echo "3. Check SSL certificate in browser - should be valid"
echo "4. Check responsive design on mobile"
echo "5. Verify modules.json loads correctly"
```

**Критерии успеха этапа 6:**
- ✅ Сервер настроен и готов к работе
- ✅ Apache2 с поддержкой поддоменов работает
- ✅ SSL сертификаты получены и настроены
- ✅ GitHub Actions деплой настроен и работает
- ✅ Основная страница платформы доступна
- ✅ Тестовый модуль работает корректно
- ✅ Все автоматические тесты проходят

**Следующий этап:** Настройка системы мониторинга и поддержки